@model MvcMusicSore.ViewModels.StoreBrowseViewModel

@{
    ViewBag.Title = "Browse";
}

<h2>Browsing Genre: @Model.Genre.Name</h2>

<ul>
    @foreach (var album in Model.Albums)
    {
        <li>@album.Title</li>
    }
</ul>

